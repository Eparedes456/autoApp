import 'package:flutter/material.dart';

class RegisUpdaPage extends StatelessWidget {
  const RegisUpdaPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: Text('Editar auto'),
      ),

      body: SingleChildScrollView(
        child: Column(
          children: [

            

          ],
        ),
      ),
      
    );
  }
}